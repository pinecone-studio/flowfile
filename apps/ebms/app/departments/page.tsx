import PlaceholderPage from '../../features/showcase/PlaceholderPage';

export default function Departments() {
  return (
    <PlaceholderPage
      title="Departments"
      description="This route now shares the new dark shell so it stays visually aligned with the redesigned documents, employees, and actions screens."
    />
  );
}
