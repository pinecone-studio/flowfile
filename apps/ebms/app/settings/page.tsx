import PlaceholderPage from '../../features/showcase/PlaceholderPage';

export default function Settings() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Settings now inherits the same navigation shell and atmospheric background so the route remains consistent with the redesigned interface."
    />
  );
}
