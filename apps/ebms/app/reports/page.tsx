import PlaceholderPage from '../../features/showcase/PlaceholderPage';

export default function Reports() {
  return (
    <PlaceholderPage
      title="Reports"
      description="Reports now open inside the same updated shell and background treatment. Add the reporting widgets here when the data slice is ready."
    />
  );
}
